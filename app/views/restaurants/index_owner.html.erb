
<%= link_to 'View your reservations', '/' %>

<ul>
<% @restaurants.each do |r| %>
<li> <%= link_to(image_tag(r.photo_url), restaurant_path(r)) %> </li>
  <li> <%= r.name %> </li>
    <li> <%= r.address %> </li>
      <li> <%= r.neighbourhood %> </li>
        <li> <%= r.price_range %> </li>
          <li> <%= r.summary %> </li>
            <li> <%= r.menu %> </li>

            </ul>

<%end%>
